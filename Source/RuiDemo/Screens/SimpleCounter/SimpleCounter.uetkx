import { UseCounter } from "./SimpleCounter.hooks"
import { CounterPanelBackground, PanelPadding, ButtonPadding, RowGap, PanelSlotPadding, ButtonSlotPadding } from "./SimpleCounter.style"

export FRuiNode SimpleCounter() {
	// const FString& Title = UseMemo<FString>([]() { return FString(TEXT("DOOM COUNTERaaaaaaaaaaaa")); }, RUI::Deps());
	auto [Count, Increment] = UseCounter(0);
	auto [Count2, Increment2] = UseState<int32>(0);

	return (
		<Border
			Padding={ PanelPadding }
			BorderImage="WhiteBrush"
			BorderBackgroundColor={ CounterPanelBackground }
			Slot.Padding={ PanelSlotPadding }
		>
			<VerticalBox>
				<TextBlock Text={ FText::FromString(TEXT("DOOM COUNTERaaaaaaaaaaaa")) } />
				<TextBlock Text={ RUI::Fmt(TEXT("Count1: {}"), Count) } />
				<TextBlock Text={ RUI::Fmt(TEXT("Count2: {}"), Count2) } />
				<Spacer Size={ RowGap } />
				<HorizontalBox>
					<Button
						OnClicked={ Increment() }
						ContentPadding={ ButtonPadding }
						Slot.Padding={ ButtonSlotPadding }
					>
						+
					</Button>
				</HorizontalBox>
				<HorizontalBox>
					<Button
						OnClicked={ Increment2([](const int32& Prev) { return Prev + 1; }) }
						ContentPadding={ ButtonPadding }
						Slot.Padding={ ButtonSlotPadding }
					>
						+
					</Button>
				</HorizontalBox>
			</VerticalBox>
		</Border>
	);
}
