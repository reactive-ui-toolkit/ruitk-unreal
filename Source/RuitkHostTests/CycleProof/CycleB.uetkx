import { CycleA } from "./CycleA"

export FRuitkNode CycleB(int32 Depth = 0) {
	return (
		<VerticalBox>
			<TextBlock Text="B" />
			@if (Depth > 0) {
				return (
					<CycleA Depth={ Depth - 1 } />
				);
			}
		</VerticalBox>
	);
}
