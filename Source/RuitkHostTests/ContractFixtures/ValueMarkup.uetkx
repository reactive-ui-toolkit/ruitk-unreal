export FRuitkNode ValueMarkup(bool bShow = false) {
	auto [Count, SetCount] = UseState(0);
	auto Card = (<VerticalBox>
		<TextBlock Text={ FText::AsNumber(Count) } />
	</VerticalBox>);
	FRuitkNode Chip = <TextBlock Text="bare" />;
	TArray<FRuitkNode> Extra;
	Extra.Add(<Spacer />);
	return (
		<Border>
			{ Card }
			{ Chip }
			{ bShow ? <TextBlock Text="on" /> : <TextBlock Text="off" /> }
		</Border>
	);
}
