// LabScopeProof.uetkx — FILE_SCOPED_EXPORTS compiled proof (the shapes the contract harness
// pins as strings, BUILT for real here): a DEFAULT import, the `~/` root-alias specifier, and
// ONE importer binding BOTH same-named `PanelBackground` exports via `as` aliases — each
// resolves to its own file's namespace (ES-module scoping; the retired 2106 forbade this).
import Badge from "./LabScopeBadge"
import { PanelBackground } from "~/Screens/SimpleCounter/SimpleCounter.style"
import { PanelBackground as ContextPanelBg } from "../../../ContextDemo/ContextDemo.style"

export FRuitkNode LabScopeProof() {
	return (
		<VerticalBox>
			<Badge />
			<Border BorderBackgroundColor={ PanelBackground } Slot.Padding="0,2,0,0">
				<Spacer Size={ FVector2D(24.f, 6.f) } />
			</Border>
			<Border BorderBackgroundColor={ ContextPanelBg } Slot.Padding="0,2,0,0">
				<Spacer Size={ FVector2D(24.f, 6.f) } />
			</Border>
		</VerticalBox>
	);
}
